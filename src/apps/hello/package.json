{
  "name": "hello-service",
  "version": "0.1.0",
  "description": "Hello service package json will have only the unique dependencies specific for the service. Mono repo main package json will have the common dependencies common to all projects",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "OpsGuru.io",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.44.0",
    "eslint": "^8.28.0",
    "eslint-config-standard-with-typescript": "^23.0.0",
    "eslint-plugin-import": "^2.26.0",
    "eslint-plugin-n": "^15.5.1",
    "eslint-plugin-promise": "^6.1.1",
    "typescript": "^4.9.3"
  }
}
